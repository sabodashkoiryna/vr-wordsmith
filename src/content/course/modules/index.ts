import type { ModuleSeed } from '../types';
import { module1 } from './m1';
import { module2 } from './m2';
import { module3 } from './m3';
import { module4 } from './m4';
import { module5 } from './m5';

/**
 * Навчальний контент курсу. Це ПОЧАТКОВЕ наповнення: сід переносить його в
 * базу, після чого канонічним джерелом стає база, а редагування йде через
 * адмінку. Правити ці файли має сенс лише щоб пересіяти курс з нуля.
 *
 * Один файл на модуль, бо в одному вони давали кілька тисяч рядків, у яких
 * неможливо орієнтуватися — а редагувати доводиться саме модулями.
 *
 * Бали: у кожному модулі лише тест, на 12 балів → 5 × 12 = 60. Решта 40 — за
 * курсовий проєкт (`../project.ts`), який лежить поза модулями. Сід перевіряє
 * цей інваріант і падає, якщо сума розійшлася.
 */
export const modules: ModuleSeed[] = [module1, module2, module3, module4, module5];
